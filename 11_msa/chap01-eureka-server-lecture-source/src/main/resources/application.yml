server:
  port: 8761
eureka:
  client:

    # eureka server: eureka server에서 관리 할 필요가 없다.
    register-with-eureka: false

    # eureka server는 다른 micro service를 30초 단위로 주기적으로 이진할 필요가 없다.
    fetch-registry: false
